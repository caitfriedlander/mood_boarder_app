<h1>New Board</h1>

<%= form_tag '/boards', method: :post do |f| %>
  <% if false#@board.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@board.errors.count, "error") %> prohibited this board from being saved:</h2>

      <ul>
      <% @board.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= label_tag "Title" %><br>
    <%= text_field_tag "board[title]" %>
  </div>
  <div class="field">
    <%= label_tag "Description" %><br>
    <%= text_field_tag "board[description]" %>
  </div>
  <div class="actions">
    <%= submit_tag "Save" %>
  </div>
<% end %>

<%= link_to 'Back', current_user %>
